{
  "name": "dealsfinder-websocket-server",
  "version": "1.0.0",
  "description": "WebSocket server for DealsFinder application",
  "main": "dist/websocket-server.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/websocket-server.js",
    "dev": "ts-node-dev --respawn websocket-server.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.4",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
